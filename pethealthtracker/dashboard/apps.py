from django.apps import AppConfig
# Imports Django's AppConfig class used to configure an application


class DashboardConfig(AppConfig):
    # Configuration class for the dashboard application

    name = 'dashboard'
    # Specifies the name of the app
    # Django uses this to identify and load the dashboard app
