from django.apps import AppConfig
# Imports Django's AppConfig class used to configure an application


class HomeConfig(AppConfig):
    # Configuration class for the home application

    name = 'home'
    # Specifies the name of the app
    # Django uses this value to identify and load the home app
